import HeaderParser from './header/HeaderParser';

import UrlReducer from './shortener/UrlReducer';

import Timestamp from './timestamp/Timestamp';

import ExerciseTracker from './exercise/ExerciseTracker';

import FileMetadata from './metadata/FileMetadata';

const index = {
    FileMetadata,
    ExerciseTracker,
    Timestamp,
    UrlReducer,
    HeaderParser
};

export default index;
